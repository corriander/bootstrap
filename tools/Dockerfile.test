FROM ubuntu:25.10

ENV DEBIAN_FRONTEND=noninteractive

RUN apt-get update \
    && apt-get install -y --no-install-recommends \
        ansible \
        git \
        python3 \
        python3-apt \
        sudo \
    && rm -rf /var/lib/apt/lists/*
